import type {ReactNode} from "react";
export type DataColumn<T> = {key: string; header: ReactNode; cell: (row: T) => ReactNode; align?: "left"|"right"};
export default function DataTable<T>({columns, rows, rowKey, caption, mobileCard}: {columns: DataColumn<T>[]; rows: T[]; rowKey: (row:T)=>string|number; caption: string; mobileCard?: (row:T)=>ReactNode}) {return <div className="min-w-0"><div className="hidden overflow-x-auto md:block"><table className="w-full border-separate border-spacing-0 text-sm"><caption className="sr-only">{caption}</caption><thead><tr>{columns.map((column)=><th className={`border-b border-stone-300 bg-stone-50 px-3 py-2 font-semibold text-stone-700 ${column.align === "right" ? "text-right":"text-left"}`} key={column.key} scope="col">{column.header}</th>)}</tr></thead><tbody>{rows.map((row)=><tr className="hover:bg-stone-50" key={rowKey(row)}>{columns.map((column)=><td className={`border-b border-stone-200 px-3 py-3 text-stone-800 ${column.align === "right" ? "text-right":"text-left"}`} key={column.key}>{column.cell(row)}</td>)}</tr>)}</tbody></table></div>{mobileCard ? <div className="grid gap-3 md:hidden">{rows.map((row)=><div key={rowKey(row)}>{mobileCard(row)}</div>)}</div>:null}</div>;}
