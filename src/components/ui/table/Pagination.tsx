import Button from "@/components/ui/button/Button";
export default function Pagination({page,totalPages,onChange,previousLabel,nextLabel,pageLabel}: {page:number;totalPages:number;onChange:(page:number)=>void;previousLabel:string;nextLabel:string;pageLabel:(page:number,total:number)=>string}) {if(totalPages<=1)return null;return <nav aria-label={pageLabel(page+1,totalPages)} className="flex flex-wrap items-center justify-between gap-3 border-t border-stone-200 py-3"><Button disabled={page<=0} onClick={()=>onChange(page-1)} size="sm" variant="secondary">{previousLabel}</Button><span className="text-sm text-stone-600">{pageLabel(page+1,totalPages)}</span><Button disabled={page>=totalPages-1} onClick={()=>onChange(page+1)} size="sm" variant="secondary">{nextLabel}</Button></nav>;}
